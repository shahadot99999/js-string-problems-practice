
//a function that will take 3 parameters and will return the min number//Using Math.min 

function minimum(num1, num2, num3) {
    return Math.min(num1, num2, num3);
}

console.log("Minimum Number:" + (Math.min(80, 70, 90)));

// write a function that will take 3 numbers will return the max number// second time do it using  Math.max
const num1 = 80;
const num2 = 70;
const num3 = 90;
function max() {
    return Math.max();
}

console.log("Maximum Number:" + (Math.max(num1, num2, num3)));